name: release
on:
  push:
    branches: [main]
  # Manual self-heal: when semantic-release tagged a version but the image
  # build/push failed (e.g. a transient buildx "unknown blob"), re-run just the
  # build & push for that already-tagged release — WITHOUT re-publishing.
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Existing release version to (re)build & push the UI image for, e.g. 1.31.0 (no leading v)."
        required: true
        type: string
permissions:
  contents: write
  packages: write
  issues: write
  pull-requests: write
concurrency:
  group: release
  cancel-in-progress: false
jobs:
  # Test gate: nothing gets tagged, released or pushed to GHCR unless the backend
  # suite (pytest) and the frontend helper tests (node --test) pass. This job runs
  # BEFORE semantic-release on purpose — a failure after tagging would leave a
  # published release with no image, which is worse than no release.
  test:
    if: github.event_name == 'push'   # manual image rebuilds re-push an already-gated tag
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22           # matches ui/Dockerfile's build stage (node:22-alpine)
          cache: npm
          cache-dependency-path: ui/frontend/package-lock.json
      - name: Frontend tests (node --test)
        working-directory: ui/frontend
        run: |
          npm ci
          npm test
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"     # matches ui/Dockerfile's runtime stage (python:3.12-slim)
          cache: pip
          cache-dependency-path: ui/pyproject.toml
      - name: Backend tests (pytest)
        working-directory: ui
        run: |
          # Install the dependency lists straight from pyproject.toml rather than
          # `pip install .`: ui/ is a flat layout holding both app/ and frontend/, so
          # setuptools refuses to auto-discover packages from a checkout (the Docker
          # build only copies app/, which is why the image build never hits this).
          python -c 'import tomllib; d = tomllib.load(open("pyproject.toml", "rb")); print("\n".join(d["project"]["dependencies"] + d["dependency-groups"]["dev"]))' > /tmp/ci-requirements.txt
          pip install -r /tmp/ci-requirements.txt
          python -m pytest -q
  release:
    needs: test
    if: ${{ !failure() && !cancelled() }}   # run when the gate passed OR was skipped (manual rebuild)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0          # full history + all tags (needed to check out a release tag on manual re-run)
      - name: Semantic release
        id: semantic
        if: github.event_name == 'push'   # never re-publish on a manual image rebuild
        uses: cycjimmy/semantic-release-action@v4
        with:
          extra_plugins: |
            @semantic-release/changelog
            @semantic-release/git
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Determine build target
        id: meta
        env:
          # Pass all context values through env (never interpolate ${{ }} into the
          # shell body) so a crafted workflow_dispatch input can't inject commands.
          EVENT_NAME: ${{ github.event_name }}
          INPUT_TAG: ${{ github.event.inputs.image_tag }}
          NEW_PUBLISHED: ${{ steps.semantic.outputs.new_release_published }}
          NEW_VERSION: ${{ steps.semantic.outputs.new_release_version }}
        run: |
          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            TAG="${INPUT_TAG#v}"                    # tolerate a leading 'v'
            if ! printf '%s' "$TAG" | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+$'; then
              echo "image_tag must be a semver like 1.31.0 (got: '$TAG')"; exit 1
            fi
            {                                        # build the tagged tree, so image == release
              echo "should_build=true"
              echo "version=$TAG"
              echo "checkout_ref=v$TAG"
            } >> "$GITHUB_OUTPUT"
          elif [ "$NEW_PUBLISHED" = "true" ]; then
            {
              echo "should_build=true"
              echo "version=$NEW_VERSION"
              echo "checkout_ref="
            } >> "$GITHUB_OUTPUT"
          else
            echo "should_build=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Check out the release tag (manual rebuild only)
        if: ${{ steps.meta.outputs.should_build == 'true' && steps.meta.outputs.checkout_ref != '' }}
        env:
          CHECKOUT_REF: ${{ steps.meta.outputs.checkout_ref }}   # already a validated v<semver>
        run: git checkout "$CHECKOUT_REF" || { echo "tag $CHECKOUT_REF not found"; exit 1; }
      - name: Set up Buildx
        if: ${{ steps.meta.outputs.should_build == 'true' }}
        uses: docker/setup-buildx-action@v3   # docker-container driver: more robust registry pushes than the default driver
      - name: Log in to GHCR
        if: ${{ steps.meta.outputs.should_build == 'true' }}
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build & push UI image (retry on transient registry errors)
        if: ${{ steps.meta.outputs.should_build == 'true' }}
        env:
          VERSION: ${{ steps.meta.outputs.version }}
        run: |
          set -u
          for attempt in 1 2 3; do
            echo "::group::build-push attempt $attempt"
            if docker buildx build \
                --push \
                --build-arg APP_VERSION="${VERSION}" \
                --tag "ghcr.io/tekgnosis-net/llm-proxy-ui:${VERSION}" \
                --tag "ghcr.io/tekgnosis-net/llm-proxy-ui:latest" \
                ./ui; then
              echo "::endgroup::"
              echo "build-push succeeded on attempt $attempt"
              exit 0
            fi
            echo "::endgroup::"
            wait=$((attempt * 20))
            echo "attempt $attempt failed; retrying in ${wait}s (transient 'unknown blob' etc.)"
            sleep "$wait"
          done
          echo "build-push failed after 3 attempts"
          exit 1
